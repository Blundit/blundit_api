<%
@expert = Expert.find(item.expert_id)
@claim = Claim.find(item.claim_id)
%>

Since you're interested in <%= @expert.name %>, we thought you might like to know that someone has attached the claim '<%= @claim.title %>' to them. 

<% if !@claim.description.nil? %>
    <%= @claim.description %>
<% end %>