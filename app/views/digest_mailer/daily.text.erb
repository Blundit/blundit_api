It's your daily digest! Here are the things that happened yesterday:

<% @items.each do |item| %>
    <%= render partial: "mailers/#{item.item_type}", locals: { item: item } %>
<% end %>

Email footer!