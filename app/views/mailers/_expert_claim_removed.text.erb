<%
@expert = Expert.find(item.expert_id)
@claim = Claim.find(item.claim_id)
%>

Since you're interested in <%= @expert.name %>, we thought you might like to know that someone has removed the claim '<%= @claim.title %>' from them. 

<% if !@claim.description.nil? %>
    <%= @claim.description %>
<% end %>