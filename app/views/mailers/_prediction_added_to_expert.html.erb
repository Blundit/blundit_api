<%
@expert = Expert.find(item.expert_id)
@prediction = Claim.find(item.prediction_id)
%>

Since you're interested in <%= @expert.name %>, we thought you might like to know that someone has attached the prediction '<%= @prediction.title %>' to them. 

<% if !@prediction.description.nil? %>
<p><%= @prediction.description %></p>
<% end %>
