<%
@expert = Expert.find(item.expert_id)
@prediction = Prediction.find(item.prediction_id)
%>

Since you're interested in <%= @prediction.title %>, we thought you might like to know that someone has attached the expert '<%= @expert.name %>' to them. 