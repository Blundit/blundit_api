<%
@claim = Claim.find(item.claim_id)
@comment = Comment.find(item.comment_id)
@commenter = User.find(@comment.user_id)
%>
A new comment on <%= @claim.title %> has been left by <%= @commenter.email %>:


<% if !@comment.title.nil? %>
    Title: <%= @comment.title %>
<% end %>
<%= @comment.content %>
