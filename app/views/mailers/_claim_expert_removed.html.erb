<%
@expert = Expert.find(item.expert_id)
@claim = Claim.find(item.claim_id)
%>

Since you're interested in <%= @claim.title %>, we thought you might like to know that someone has removed the expert '<%= @expert.name %>' from it. 