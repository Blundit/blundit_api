<%
@claim = Claim.find(item.claim_id)
@expert = Expert.find(item.expert_id)

if @claim.vote_value >= 0.5
    @status = 'TRUE'
else
    @status = 'FALSE'
end
%>

<p>You're following <%= @expert.name %>, and a claim they've made, '<%= @claim.title %>', has just been marked as <strong><%= @status %></strong> by the community. We thought you'd like to know.</p>