<%
@prediction = Prediction.find(item.prediction_id)
@comment = Comment.find(item.comment_id)
@commenter = User.find(@comment.user_id)
%>
A new comment on <%= @prediction.title %> has been left by <%= @commenter.email %>:

<% if !@comment.title.nil? %>Title: <%= @comment.title %><% end %>
<%= @comment.content %>
