<%
@claim = Claim.find(item.claim_id)
%>

The claim you're following, '<%= @claim.title %>', has been updated.

Not sure how to record exactly what's been changed, though.

<% if !item.message.nil? %>
    <%= item.message %>
<% end %>